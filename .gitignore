# Per-machine runtime state — PIDs, last-up timestamps, service status.
# Each rig writes its state to <id>.state/ alongside <id>.json. Not portable
# across machines.
*.state/

# Local benchmark outputs and artifacts.
.bench-output/

# macOS Finder metadata.
.DS_Store
